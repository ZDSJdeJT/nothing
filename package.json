{
  "name": "nothing",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun --bun run src/cli.ts",
    "test": "bun --bun test",
    "build": "bun --bun build src/index.ts --compile --minify --sourcemap --outfile dist/nothing",
    "format": "bunx --bun @biomejs/biome format ./ --write",
    "lint": "bunx --bun @biomejs/biome lint ./ --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
